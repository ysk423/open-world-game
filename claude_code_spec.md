# 実装仕様書 — ほのぼの2Dドット絵オープンワールド(ブラウザ・オンラインマルチ)

このドキュメントは、Claude Codeに実装を依頼するための仕様書です。企画の背景説明は最小限にし、実装に必要な情報を中心にまとめています。

**使い方**: 「実装フェーズ」の章を上から順番に、1フェーズずつClaude Codeに渡してください。各フェーズには受け入れ基準(このフェーズが完了したと言える条件)と、そのままコピペで使える依頼文を用意しています。

---

## 1. プロダクト概要

- ブラウザで動く、2Dドット絵の見下ろし型オープンワールドゲーム。
- 友達2〜4人でオンラインのルームに入り、1つの拠点を共有しながら、周辺エリアを探索して素材を集め、拠点を育てていく。
- テーマは「ほのぼの日常系」。失敗してもペナルティが軽く、詰みが発生しない設計。
- 目的は**動く試作(MVP)を素早く作ること**。作り込みより「一通り遊べる状態」を優先する。

---

## 2. スコープ(MVP)

### 含める
- 拠点(小屋1つ)+周辺2〜3チャンクの探索可能マップ
- 2〜4人が同じルームに入り、お互いの位置・向き・アニメーションがリアルタイムに見える
- 採集ポイント3種類程度、プレイヤーごとの個人インベントリ
- クラフトレシピ5〜8個程度(作った結果は拠点に反映され、全員が見える/使える)
- 小型モンスター1種類との簡易戦闘(接触 or ボタン攻撃)
- NPC1〜2人との簡易会話(短いテキスト表示のみ)
- 拠点拡張アイテムを作る→隣接チャンクが解放される、という最小限の進行システム
- キーボード(WASD/矢印キー)での移動、マウス/トラックパッドのクリックでのアクション

### 含めない(MVP対象外。フェーズ6以降の課題として明記のみ)
- 昼夜サイクル・天候
- フル会話ツリー・NPC好感度システム
- ボス戦・複数モンスター種
- サーバー側の厳密なチート対策(友達内輪プレイの前提で簡易同期にとどめる)
- スマホのタッチ操作UI本体(ただし入力層は差し替え可能な設計にしておく。詳細は7.9)
- アカウント認証・ログイン機能(名前入力のみで入室可)
- 実績・図鑑などのメタ進行

---

## 3. アーキテクチャ概要

```
[ブラウザ クライアント]                [Cloudflare]
 Phaser 3 + TypeScript                Workers + Durable Objects
 (Vite でビルド)          <--WS-->    1ルーム = 1 Durable Object
        |                                    |
   Cloudflare Pages                    自動起動/自動休止
   (静的ホスティング)                  (常時起動のサーバー不要)
```

- クライアント: Phaser 3(TypeScript)。Tiledで作ったタイルマップ(JSON)を読み込む。
- マルチプレイサーバー: Cloudflare Workers上のDurable Objects。1ルーム=1 Durable Objectとして、参加者の位置・拠点の状態を保持する。`partyserver`ライブラリ(旧PartyKitチーム製、Cloudflareが提供)の利用を推奨。
- ルームは誰かが入室したときに自動生成され、全員退出すれば自動的に休止する。開発者・プレイヤーのPCやサーバーを常時起動しておく必要はない。
- クライアントはCloudflare Pages、サーバーはCloudflare Workersにデプロイする。両方ともCloudflareの無料枠内で開発を始められる。

---

## 4. 技術スタック

| レイヤー | 技術 | 備考 |
|---|---|---|
| ゲームエンジン | Phaser 3 | TypeScriptで記述 |
| ビルドツール | Vite | クライアント側 |
| マルチプレイサーバー | Cloudflare Workers + Durable Objects(`partyserver`推奨) | 1ルーム=1 Durable Object |
| 通信方式 | WebSocket | 位置同期はリアルタイム、拠点状態変更はイベント単位 |
| マップ制作 | Tiled(外部ツール) | JSON形式で書き出し、Phaserで読み込み |
| クライアントホスティング | Cloudflare Pages | 静的ファイルとして配信 |
| サーバーホスティング | Cloudflare Workers | `wrangler`でデプロイ |
| 言語 | TypeScript(クライアント・サーバー共通) | 型を共有しデータ構造の齟齬を防ぐ |

**注意**: Node.js常駐サーバーが前提のフレームワーク(例: Colyseusなど)は、Cloudflare Workersのランタイム(V8アイソレート、Node.js API非対応)ではそのまま動作しない。必ずCloudflare Workers/Durable Objectsで動作する構成(`partyserver`など)を使うこと。

---

## 5. ディレクトリ構成(想定)

```
/client                  # Phaserゲーム本体(Cloudflare Pagesにデプロイ)
  /src
    /scenes               # Phaserのシーン
    /input                # InputManager等、入力抽象化層(7.9参照)
    /net                   # WebSocket通信・状態同期まわり
    /entities              # プレイヤー、NPC、モンスターなど
  /public
    /maps                  # Tiledで書き出したJSON
    /assets                # タイルセット、スプライト、音声
  vite.config.ts

/server                  # Cloudflare Workers(Durable Objects)
  /src
    room.ts                # ルーム(Durable Object)のロジック
    index.ts                # Worker エントリポイント
  wrangler.toml

/docs
  claude_code_spec.md      # 本ドキュメント
```

Claude Codeは上記を目安にしつつ、必要に応じて構成を調整してよい。

---

## 6. ゲームデザイン仕様

### 6.1 世界観
「はじまりの湾」という入り江を中心にした島嶼エリア。森・草原・岩場・小さな遺跡が点在する、ゆるやかなファンタジー寄りの田舎が舞台。トーンは明るく牧歌的。

### 6.2 コアループ
1. 拠点で準備(装備・道具・畑を確認)
2. 探索に出る(各自バラバラでも一緒でもよい。マップを歩き、未開拓エリアを見つける)
3. 軽い脅威に遭遇(小型モンスター。1人でも複数人でも対処できる強さ)
4. 素材を拠点に持ち帰る
5. クラフトで拠点を拡張(道具強化・拠点拡張アイテム作成)
6. 拠点拡張により行動範囲が広がる

### 6.3 マップ設計
- チャンク単位のタイルマップ(1チャンク = 16x16〜32x32マス程度)。
- 最初は拠点周辺の3〜4チャンクのみ開放。
- 未探索エリアの可視化は**共有フォグ**(誰か1人が見た場所は全員に見える)でよい。個別フォグより実装がシンプル。

### 6.4 探索・収集
- 採集ポイント(木・岩・草・貝など)3種類程度をマップに配置。
- 隠しアイテムや簡易な障害物(岩をどかす、橋をかける等)を1〜2個用意。
- 採集したアイテムは各プレイヤーの個人インベントリに入る。

### 6.5 クラフト・拠点(共有)
- 拠点は全プレイヤー共有。誰かが作った建物・畑・家具は他の全員から見える・使える。
- レシピは「素材A×2 + 素材B×1 → アイテムC」のようなシンプルな表引き構造。5〜8個程度。
- 拠点拡張アイテムを作ると、隣接チャンクへの道が開く(探索範囲が広がる)。

### 6.6 戦闘・アクション
- 小型モンスター1種類。接触ダメージ、またはボタン入力での簡易攻撃。
- 倒せなくても回避して進めるルートを必ず用意する。
- 死亡してもゲームオーバーにはせず、軽いペナルティ(足止め程度)で復帰できるようにする。

### 6.7 NPC
- 1〜2人のNPCを町に配置。話しかけると短いテキストが表示される程度でよい(選択肢・好感度はMVP対象外)。

### 6.8 マルチプレイ仕様
- 1ルームあたり最大4人。友達内輪でルームIDを共有して入室する想定(認証機能なし、名前入力のみ)。
- 同期対象: プレイヤーの位置・向き・アニメーション状態(リアルタイム)、拠点の建物・畑・共有倉庫の状態(イベント単位)。
- クライアント側で他プレイヤーの位置を滑らかに表示するため、受信した位置情報を補間(interpolation)して描画する。
- サーバー側の厳密な不正対策は不要(信頼できる友達内輪プレイが前提)。将来的に必要になれば追加する。

### 6.9 操作方法
- 移動: キーボード(WASD/矢印キー)。
- アクション(採集・攻撃・メニュー選択など): マウス/トラックパッドのクリック。
- 将来的にスマホのタッチ操作(仮想スティック+タップ)にも対応する予定のため、**入力処理はゲームロジックから分離した`InputManager`のような層として実装する**こと。ゲーム本体のロジックは「移動方向」「アクション実行」といった抽象的な入力イベントのみを受け取り、キーボード/マウス/将来のタッチ入力のどれから来たかを意識しない設計にする。

### 6.10 アートスタイル仕様
- タイルサイズ: 16×16px。
- キャラクターサイズ: 16×16〜16×32px程度。
- 内部解像度: 320×180 または 384×216程度の低解像度で描画し、CSSで`image-rendering: pixelated`を指定してピクセルパーフェクトに拡大表示する。
- パレット: エリアごとに8〜16色程度に抑える。
- 初期段階はフリー/購入素材のタイルセット・スプライトを使用してよい(自作ドット絵は後回し)。
- 他プレイヤーと見分けやすいよう、キャラクターに簡単な色バリエーション(帽子の色など)を用意する。

---

## 7. データモデル(ラフ仕様)

実装の出発点として、以下のようなデータ構造を想定している。詳細はClaude Codeの実装時に調整してよい。

```typescript
// プレイヤーの状態(サーバー・クライアント間で同期)
type PlayerState = {
  id: string;
  name: string;
  x: number;
  y: number;
  direction: "up" | "down" | "left" | "right";
  animState: "idle" | "walk" | "attack";
  inventory: InventoryItem[];
};

type InventoryItem = {
  itemId: string;
  quantity: number;
};

// 拠点(ルームで共有される状態)
type BaseState = {
  buildings: PlacedBuilding[];
  sharedStorage: InventoryItem[];
  unlockedChunks: string[]; // 解放済みチャンクIDのリスト
};

type PlacedBuilding = {
  buildingId: string;
  x: number;
  y: number;
};

// クラフトレシピ
type Recipe = {
  id: string;
  inputs: InventoryItem[];
  output: InventoryItem;
};

// ルーム全体の状態(Durable Objectが保持)
type RoomState = {
  players: Record<string, PlayerState>;
  base: BaseState;
};
```

---

## 8. 実装フェーズ & 受け入れ基準

### フェーズ0: 1人プレイの土台
**受け入れ基準**: ブラウザでゲームを開くと、16x16pxのキャラクターがタイルマップ上をWASD/矢印キーで自由に歩き回れる。カメラがキャラクターに追従する。

**依頼文**:
```
ブラウザで動く2Dドット絵オープンワールドゲームの試作を作りたい。
最終的には友達2〜4人でのオンラインマルチプレイに対応する予定だが、
まずは1人プレイの土台を作ってほしい。

- Vite + TypeScript + Phaser 3 の構成
- 内部解像度は384x216程度、image-rendering:pixelatedでピクセルパーフェクト表示
- Tiledで作成したタイルマップ(JSON)を読み込める構成にする
  (今はサンプルの仮タイルマップでOK。後で差し替える)
- 16x16pxのプレイヤーキャラクターをWASD/矢印キーで上下左右に移動できる
- 採集・攻撃・メニュー選択などのアクションはマウス/トラックパッドのクリックで
  行えるようにする(将来タッチ操作にも差し替えられるよう、入力処理は
  InputManagerのような形で他のロジックから分離しておく)
- カメラはプレイヤーに追従する
- README.mdに、ローカルでの起動方法と、ディレクトリ構成の説明を書く

まずは「マップの上をキャラが歩き回れる」ところまでを動く状態にしてください。
```

### フェーズ1: マルチプレイ基盤
**受け入れ基準**: 2つ以上のブラウザタブ(またはウィンドウ)で同じルームに入ると、互いのキャラクターがリアルタイムに動いて見える。

**依頼文**:
```
フェーズ0で作った1人プレイのPhaserゲームに、Cloudflare Workers上の
Durable Objectsを使ったオンラインマルチプレイの基盤を追加してほしい。
自分のPCやどこかのサーバーを常時起動しておく必要がない構成にしたい。

- Cloudflare Workers + Durable Objects でルームサーバーを構築する
  (PartyKitチームが提供している partyserver ライブラリの利用を検討してよい)
- 1ルーム = 1つのDurable Objectとし、最大4人まで参加できる
- 各プレイヤーの位置・向き・アニメーション状態をリアルタイムに
  WebSocket経由で他のプレイヤーに同期する
- クライアント側は、他プレイヤーのキャラクターを画面上に表示し、
  滑らかに動くよう補間(interpolation)する
- ルームIDを指定して参加できる簡易な参加画面を用意する
  (認証やアカウント機能は今回は不要。名前だけ入力して入室でOK)
- ローカル開発時は`wrangler dev`等で複数タブを開いて動作確認できるようにする
- README.mdに、ローカルでの動作確認方法と、Cloudflareへのデプロイ手順
  (`wrangler deploy`等)を追記する
```

### フェーズ2: 探索・収集
**受け入れ基準**: マップ上の採集ポイントに近づいてアクションすると、個人インベントリにアイテムが追加される。マップ端に到達すると別チャンクに移動できる。

**依頼文**:
```
現在のゲームに、探索・収集の要素を追加してほしい。

- マップ上に採集ポイント(木・岩・草など3種類程度)を配置し、
  近づいてクリック(アクション)するとアイテムが手に入る
- 各プレイヤーは個人のインベントリを持ち、取得したアイテムが加算される
  (画面上にインベントリの中身を表示するシンプルなUIを追加)
- マップの端に到達すると、隣接するチャンク(別のタイルマップ)に
  シームレスに移動できるようにする
- 取得したアイテムの種類・個数はリロードしても保持されるよう、
  最低限のセーブ(localStorageやDurable Object側の永続化)を検討する
```

### フェーズ3: クラフト・拠点(共有)
**受け入れ基準**: 集めた素材を使ってクラフトメニューからアイテムを作成でき、作成した建物・拡張が全プレイヤーの画面に反映される。

**依頼文**:
```
現在のゲームに、クラフトと拠点拡張の要素を追加してほしい。

- クラフトメニューUIを追加し、レシピ(素材A×2+素材B×1→アイテムC 等、
  5〜8個程度)に従ってアイテムを作成できるようにする
- 拠点は全プレイヤー共有とし、誰かが建物や拡張アイテムを作ると、
  他の全プレイヤーの画面にも即座に反映される(Durable Object側の
  共有状態として管理する)
- 拠点拡張アイテムを作成すると、隣接する未開放チャンクへの道が開く
  (フェーズ2で作ったチャンク移動の仕組みと連動させる)
```

### フェーズ4: 戦闘
**受け入れ基準**: 小型モンスターに接触または攻撃アクションでダメージを与えられ、倒すと消える。プレイヤーがダメージを受けても大きなペナルティなく続行できる。

**依頼文**:
```
現在のゲームに、簡易な戦闘要素を追加してほしい。

- 小型モンスターを1種類、マップ上の特定エリアに配置する
- プレイヤーは攻撃アクション(クリック)でモンスターにダメージを与えられる
- モンスターもプレイヤーに接触するとダメージを与える
- プレイヤーの体力が0になっても「ゲームオーバー」にはせず、
  拠点に戻る/一定時間動けなくなる程度の軽いペナルティで復帰できるようにする
- モンスターを倒さなくても回避して先に進めるルートを確保する
```

### フェーズ5: 仕上げ
**受け入れ基準**: タイトル画面からルームに参加でき、NPCと簡単な会話ができ、BGM/効果音が鳴る。

**依頼文**:
```
現在のゲームに、仕上げの要素を追加してほしい。

- タイトル画面(名前入力+ルームID入力で参加)を追加する
- NPCを1〜2人配置し、近づいて話しかけると短いテキストが表示されるようにする
- BGMと簡単な効果音(採集・攻撃・クラフト完了時など)を追加する
- README.mdを最新の構成・遊び方に合わせて更新する
```

---

## 9. 非機能要件・注意点

- コスト: Cloudflare Pages/Workersの無料枠内での開発・小規模運用を前提とする。
- チート対策: 友達内輪の信頼できるプレイが前提のため、MVPでは厳密なサーバー権威型の検証は行わない。
- パフォーマンス: 同時接続は最大4人程度を想定しており、高頻度・大規模な同期最適化は不要。
- 各フェーズはできるだけ独立してテスト可能な単位で区切ってあるため、フェーズごとに動作確認してから次に進むこと。

---

## 10. 未決定事項(実装時に仮決めしてよい)

以下は明確な決定がまだないため、Claude Code側で一般的なベストプラクティスに沿って仮決めしてよい(後で変更可能な形にしておくこと)。

- 主人公・NPCの名前、ゲームタイトル名
- 最初のモンスターの見た目イメージ
- ルーム参加時のUI/UXの細部(URL共有のみか、簡単なパスコードを設けるか)
